# Where the memory tree lives inside the backend container/VM.
# For local dev this default fits your repository layout.
MEMORY_ROOT=ReactViewer/public/memory

# Comma-separated origins for CORS (your Netlify/Vercel app URLs)
CORS_ALLOW_ORIGINS=http://localhost:5173,https://yourdashboard.netlify.app

# Optional AWS S3 backup settings (stub – implement if you want daily backups)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=eu-west-1
S3_BUCKET=livelex-backups
